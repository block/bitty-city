pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "bitty-city"

include(":innie")
include(":outie")
include(":outie-jooq-provider")
